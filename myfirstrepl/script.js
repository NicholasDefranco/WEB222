
var length;
var width;
var area;

length = prompt("Enter the length");
width = prompt("Enter the width");

area = length * width;

alert(`The area of the rectangle with the dimensions : 
          ${length} and ${width} is: ${area}`);
